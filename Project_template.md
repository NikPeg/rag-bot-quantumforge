# Проектная работа: RAG-бот для QuantumForge Software

## Задание 1. Исследование моделей и инфраструктуры

### Отчёт
📄 [Полный отчёт исследования](./task_1/research_report.md)

### Краткие выводы

#### Выбранная конфигурация

**Рекомендуемое решение (для российских компаний):**
- **LLM**: **YandexGPT** (базовая версия, через HTTP API) — оптимальный баланс скорости и стоимости
- **Эмбеддинги**: sentence-transformers/all-MiniLM-L6-v2 (локально)
- **Векторная БД**: ChromaDB
- **Сервер**: Yandex Cloud Compute (8 vCPU, 32 GB RAM)

**Обоснование выбора YandexGPT:**
- ✅ Лучший баланс скорость/стоимость: 0.91 сек, ₽0.0155/запрос
- ✅ Гарантированная доступность из РФ (официальный сервис Yandex)
- ✅ Отличная поддержка русского языка
- ✅ Конфиденциальность (данные обрабатываются в России/СНГ)
- ✅ Простота интеграции через HTTP API

**Альтернативные варианты:**
- **YandexGPT Lite**: Максимальная экономия (₽0.00875/запрос), но немного медленнее (0.96 сек)
- **YandexGPT 5 Pro**: Максимальное качество (0.79 сек), но дороже в 1.5 раза (₽0.02145/запрос)
- **Xiaomi MiMo-V2-Flash**: Бесплатно, но медленнее (~4.5 сек) и зависимость от OpenRouter
- **Google Gemini 2.5 Flash**: Оптимальный баланс для международных компаний (~1.9 сек, $0.0003/запрос)
- **DeepSeek V3.2**: Максимальная экономия ($0.000054/запрос), но медленное (~8 сек)

#### Результаты практического тестирования

**Модели через OpenRouter API:**

| Модель | Время (с) | Стоимость/запрос | Доступность из РФ |
|--------|-----------|------------------|-------------------|
| Xiaomi MiMo-V2-Flash | 4.53 | **$0.000000** | ✅ |
| Google Gemini 2.5 Flash | 1.88 | $0.000287 | ✅ |
| Google Gemini 3 Flash Preview | 1.83 | $0.000362 | ✅ |
| Claude Sonnet 4.5 | 4.55 | $0.003351 | ✅ |
| DeepSeek V3.2 | 7.97 | **$0.000054** | ✅ |

**Модели YandexGPT (через HTTP API):**

| Модель | Время (с) | Стоимость/запрос (₽) | Доступность из РФ |
|--------|-----------|----------------------|-------------------|
| **YandexGPT** | **0.91** | **₽0.0155** | ✅ |
| YandexGPT Lite | 0.96 | **₽0.00875** | ✅ |
| YandexGPT 5 Pro | 0.79 | ₽0.02145 | ✅ |
| Alice AI | 1.47 | ₽0.0184 | ✅ |

**Ключевые выводы:**
1. **YandexGPT (базовая версия)** — рекомендовано для российских компаний: лучший баланс скорости (0.91 сек) и стоимости (₽0.0155/запрос)
2. Все протестированные модели доступны из РФ
3. YandexGPT Lite — самое дешёвое решение (₽0.00875/запрос), подходит для экономных сценариев
4. YandexGPT 5 Pro — самое быстрое (0.79 сек), но дороже в 1.5 раза
5. Для международных компаний: Google Gemini 2.5 Flash — оптимальный выбор (быстро + дёшево)
6. Для тестирования: Xiaomi MiMo-V2-Flash — полностью бесплатно

#### Выбранная векторная БД

**ChromaDB** — выбран как основная векторная БД по следующим причинам:
- ✅ Встроенная поддержка метаданных (критично для трассировки источников)
- ✅ Простота использования и интеграции с LangChain
- ✅ Автоматическая персистентность
- ✅ Достаточная производительность для текущего объёма данных (200,000-300,000 чанков)

**Альтернатива:** FAISS — быстрее, но требует ручной работы с метаданными

#### Конфигурация сервера

**Рекомендуемая конфигурация:**
- **CPU**: 8-16 ядер
- **RAM**: 32 GB (с запасом для роста)
- **GPU**: не требуется (используем облачные LLM)
- **Диск**: 100 GB SSD
- **Стоимость**: ~$200-300/мес (Yandex Cloud)

### Файлы задания

- `task_1/research_report.md` — полный отчёт исследования
- `task_1/test_openrouter_models.py` — скрипт для тестирования моделей через OpenRouter
- `task_1/test_yandex_gpt.py` — скрипт для тестирования YandexGPT через HTTP API
- `task_1/openrouter_test_results.json` — результаты тестирования моделей через OpenRouter
- `task_1/yandex_gpt_test_results.json` — результаты тестирования моделей YandexGPT
- `task_1/README.md` — инструкции по использованию

---

## Задание 2. Подготовка базы знаний

### Отчёт

База знаний создана на основе вики Hazbin Hotel с полной заменой всех оригинальных терминов на вымышленные.

### Выбранная предметная область

**Источник**: Вики Hazbin Hotel (https://hazbinhotel.fandom.com)

**Обоснование выбора**:
- Популярный фандом, который точно присутствует в обучающих данных LLM
- Богатая вселенная с множеством персонажей, локаций и концепций
- Достаточный объём контента для создания полноценной базы знаний

### Результаты скачивания и обработки

**Скачано страниц**: 173 документа из вики
- Персонажи (Characters): 50 страниц
- Локации (Locations): 50 страниц
- Эпизоды (Episodes): 49 страниц
- Объекты (Objects): 15 страниц
- Организации (Organizations): 19 страниц
- События (Events): 6 страниц

**Обработано документов**: 174 файла (включая README)
- Формат: Markdown (.md)
- Общий объём: ~1.66 млн символов текста
- Все документы очищены от HTML и лишнего форматирования

### Словарь замен

**Файл**: `knowledge_base/terms_map.json`

**Категории замен**:
- **Персонажи** (58 терминов): Charlie → Zara, Vaggie → Mira, Alastor → Korax, Angel Dust → Silk Shadow и т.д.
- **Локации** (25 терминов): Hazbin Hotel → Twilight Manor, Hell → Netherrealm, Heaven → Celestia и т.д.
- **Концепции** (25 терминов): Extermination → Purge, Sinners → Fallen, Angels → Stokers и т.д.
- **Организации** (8 терминов): The Overlords → The Sovereigns, Exterminators → Purifiers и т.д.
- **Предметы** (6 терминов): Angelic Steel → Stoker Steel, Holy Water → Divine Water и т.д.
- **Серии** (1 термин): Helluva Boss → Nether Agents

**Всего замен**: 123+ термина

**Особенности замены**:
- Использованы нейтральные термины, не связанные с оригинальным лором (например, "Stoker" вместо "Seraph" для ангелов)
- Избеганы конфликты с существующими персонажами из других фан-вики
- Сохранена логика и стиль мира для читаемости текстов

### Скрипты обработки

1. **`download_wiki.py`** — автоматическое скачивание страниц вики
   - Использует Fandom API для получения списка страниц
   - Парсит HTML и извлекает чистый текст
   - Сохраняет в формате Markdown

2. **`replace_terms.py`** — замена всех терминов согласно словарю
   - Обрабатывает формы с апострофами (Charlie's → Zara's)
   - Сортирует замены по длине для избежания частичных замен
   - Поддерживает регистронезависимую замену

3. **`create_wordcloud.py`** — создание облака слов для проверки
   - Визуализация частоты терминов
   - Проверка на наличие незаменённых оригинальных терминов
   - Результат: `wordcloud.png`

### Структура базы знаний

```
knowledge_base/
├── raw/                    # Исходные скачанные страницы (173 файла)
├── *.md                    # Обработанные документы с заменёнными терминами (173 файла)
├── terms_map.json          # Словарь замен
├── download_wiki.py        # Скрипт скачивания вики
├── replace_terms.py        # Скрипт замены терминов
├── create_wordcloud.py     # Скрипт создания облака слов
├── wordcloud.png           # Облако слов для проверки
├── requirements.txt        # Зависимости Python
└── README.md               # Документация
```

### Примеры замен

| Оригинал | Замена | Категория |
|----------|--------|-----------|
| Charlie | Zara | Персонаж |
| Hazbin Hotel | Twilight Manor | Локация |
| Hell | Netherrealm | Локация |
| Angels | Stokers | Концепция |
| Extermination | Purge | Концепция |
| Blitzo | Spike | Персонаж |

### Валидация

✅ **Проверка замены терминов**: Все оригинальные термины успешно заменены в содержимом документов
✅ **Облако слов**: Создано и проверено — оригинальные термины отсутствуют
✅ **Объём базы**: 174 документа (превышает требование 30+)
✅ **Формат**: Markdown (.md) — удобен для дальнейшей обработки
✅ **Словарь замен**: Сохранён в `terms_map.json` с полным описанием

### Файлы задания

- `knowledge_base/` — папка с базой знаний (174 документа)
- `knowledge_base/terms_map.json` — словарь замен
- `knowledge_base/download_wiki.py` — скрипт скачивания вики
- `knowledge_base/replace_terms.py` — скрипт замены терминов
- `knowledge_base/create_wordcloud.py` — скрипт создания облака слов
- `knowledge_base/wordcloud.png` — облако слов для проверки
- `knowledge_base/README.md` — инструкции по использованию

---

## Задание 3. Создание векторного индекса базы знаний

### Отчёт

Векторный индекс базы знаний успешно создан с использованием ChromaDB и модели эмбеддингов `sentence-transformers/all-MiniLM-L6-v2`.

### Выбранная модель эмбеддингов

**Модель**: `sentence-transformers/all-MiniLM-L6-v2`

**Характеристики**:
- **Размерность эмбеддингов**: 384
- **Размер модели**: ~22 МБ
- **Скорость**: ~5-10 мс на CPU для короткого предложения
- **Репозиторий**: https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2
- **Обоснование**: Выбрана согласно рекомендациям из задания 1 — оптимальный баланс качества, скорости и размера для локального использования

### Параметры разбиения на чанки

- **Размер чанка**: 500 токенов (в диапазоне 500-1000 токенов, как рекомендовано в задании)
- **Overlap**: 100 токенов (20% перекрытия для сохранения контекста)
- **Разделители**: `\n\n`, `\n`, `. `, ` `, `""` (RecursiveCharacterTextSplitter)
- **Метод**: Использован `RecursiveCharacterTextSplitter` из LangChain для логического разбиения текста

### Метаданные

Каждый чанк содержит следующие метаданные для трассировки источников:
- `source`: полный путь к исходному файлу
- `file_name`: имя файла
- `chunk_index`: порядковый номер чанка в документе (начиная с 0)
- `total_chunks`: общее количество чанков в документе

### Векторная БД

**ChromaDB** — выбрана согласно рекомендациям из задания 1:
- ✅ Встроенная поддержка метаданных (критично для трассировки источников)
- ✅ Автоматическая персистентность на диск
- ✅ Простая интеграция с LangChain
- ✅ Достаточная производительность для текущего объёма данных

### Результаты индексации

**База знаний**:
- Использована база знаний из задания 2 (Hazbin Hotel с заменёнными терминами)
- Обработано: ~347 документов в формате Markdown
- Исключены: папка `raw` и служебные файлы

**Процесс индексации**:
1. Загрузка всех `.md` документов из `knowledge_base/`
2. Разбиение документов на чанки с сохранением метаданных
3. Генерация эмбеддингов для каждого чанка
4. Сохранение индекса в ChromaDB на диск

**Статистика** (результаты первого запуска):
- Количество обработанных документов: **173 документа** из базы знаний задания 2
- Количество созданных чанков: **4509 чанков**
- Время индексации: **30.39 секунд**
- Средняя скорость обработки: **148.35 чанков/сек**

### Тестирование индекса

Реализованы тестовые запросы для проверки качества индекса:
- "Что такое Eclipse Manor?"
- "Кто такой Korax?"
- "Опиши Netherrealm"

Каждый запрос возвращает топ-3 наиболее релевантных чанка с оценкой релевантности (score) и метаданными.

### Структура файлов

```
task_3/
├── build_index.py      # Скрипт создания векторного индекса
├── query_index.py      # Скрипт для тестирования и использования индекса
├── README.md           # Подробная документация по использованию
├── requirements.txt    # Зависимости Python
└── chroma_db/          # Созданный векторный индекс (после запуска)
```

### Использование

**Создание индекса**:
```bash
cd task_3
pip install -r requirements.txt
python build_index.py
```

**Тестирование индекса**:
```bash
python query_index.py
```

### Файлы задания

- `task_3/build_index.py` — скрипт для создания векторного индекса
- `task_3/query_index.py` — скрипт для тестирования и использования индекса
- `task_3/README.md` — подробная документация
- `task_3/requirements.txt` — зависимости Python
- `task_3/chroma_db/` — созданный векторный индекс (после запуска)

---

## Задание 4. Реализация RAG-бота с техниками промптинга

### Отчёт

Реализован полнофункциональный RAG-бот с применением техник промптинга: Few-shot и Chain-of-Thought (CoT). Бот успешно интегрирован с векторной базой знаний и YandexGPT API.

### Реализованные компоненты

**Основной модуль RAG-бота** (`rag_bot.py`):
- ✅ Загрузка векторного индекса из ChromaDB (создан в задании 3)
- ✅ Поиск релевантных чанков по запросу пользователя
- ✅ Интеграция с YandexGPT через HTTP API
- ✅ Формирование промптов с Few-shot примерами и Chain-of-Thought
- ✅ Обработка ошибок и случаев отсутствия информации

**Техники промптинга:**

1. **Few-shot prompting**:
   - Автоматическое извлечение 1-2 примеров вопросов-ответов из базы знаний
   - Примеры выбираются на основе релевантности к типичным запросам
   - Показывают модели желаемый формат ответа

2. **Chain-of-Thought (CoT)**:
   - Пошаговое рассуждение перед ответом
   - Модель показывает, как она анализирует контекст
   - Формат: "[Рассуждение] 1. Ищу... 2. В документе найдено... 3. Следовательно..."
   - Можно включать/отключать через параметр `use_cot`

### Архитектура RAG-пайплайна

Процесс обработки запроса состоит из следующих этапов:

1. **Загрузка индекса**: Векторный индекс загружается из ChromaDB
2. **Поиск релевантных чанков**: Запрос преобразуется в эмбеддинг, ищутся ближайшие чанки (топ-5)
3. **Формирование промпта**:
   - System prompt с инструкциями и правилами
   - Few-shot примеры (1-2 примера из базы знаний)
   - Контекст (найденные чанки с указанием источников)
   - Запрос пользователя
4. **Генерация ответа**: Промпт отправляется в YandexGPT
5. **Возврат результата**: Ответ возвращается с указанием источников и метаданных

### Используемые технологии

- **Векторная БД**: ChromaDB (индекс из задания 3)
- **Модель эмбеддингов**: `sentence-transformers/all-MiniLM-L6-v2`
- **LLM**: YandexGPT (базовая версия, через HTTP API)
- **Библиотеки**: LangChain, ChromaDB, requests

### Результаты тестирования

Проведено автоматическое тестирование всех функций бота:

**Тест 1: Успешные ответы на вопросы из базы знаний**
- ✅ 4/4 запросов обработано корректно
- Примеры: "Что такое Twilight Manor?", "Кто такой Korax?", "Опиши Netherrealm", "Что происходит во время Purge?"
- Бот даёт информативные ответы с использованием Chain-of-Thought
- Все примеры соответствуют требованию задания (3-5 успешных диалогов)

**Тест 2: Корректные ответы "Я не знаю"**
- ✅ 4/4 запросов обработано корректно
- Примеры: "Как называется столица Марса?", "Кто такой Гарри Поттер?", "Что такое квантовая физика?"
- Бот честно отвечает "Я не знаю" на вопросы вне базы знаний
- Соответствует требованию задания (1-2 случая "Я не знаю")

**Тест 3: Проверка Chain-of-Thought**
- ✅ Работает корректно
- В ответах присутствуют признаки рассуждения: "Ищу в контексте...", "В документе найдено...", "Следовательно..."

**Тест 4: Проверка Few-shot примеров**
- ✅ 2 примера загружены успешно
- Примеры автоматически извлекаются из базы знаний

### Примеры работы

**Пример 1: Успешный ответ с Chain-of-Thought**

```
Запрос: Что такое Twilight Manor?

Ответ:
[Рассуждение]
1. Ищу в контексте информацию о Twilight Manor.
2. В документе [1] найдено: «Twilight Manor, formerly known as the Happy Hotel, 
   is a hotel run by Zara Morningstar who are undergoing rehabilitation to stay at. 
   It is located in Starfall City».
3. В документе [5] найдено описание внешнего вида Twilight Manor...
4. Следовательно, ответ будет содержать основную информацию...

[Ответ]
Twilight Manor — это отель, которым управляет Zara Morningstar...
```

**Пример 2: Ответ "Я не знаю"**

```
Запрос: Кто такой Гарри Поттер?

Ответ: Я не знаю. В базе знаний не найдено информации по вашему запросу.
Найдено чанков: 0
```

### Интерфейсы

1. **REPL (интерактивный режим)**: `python rag_bot.py`
   - Интерактивный диалог с ботом
   - Команды: `exit`, `cot`, `nocot`

2. **Программный API**: можно импортировать и использовать в коде
   ```python
   from rag_bot import RAGBot
   bot = RAGBot()
   result = bot.answer("Ваш вопрос", use_cot=True)
   ```

### Структура файлов

```
task_4/
├── rag_bot.py          # Основной модуль RAG-бота (720 строк)
├── test_bot.py         # Скрипт для автоматического тестирования (188 строк)
├── demo.py             # Демонстрация работы бота (75 строк)
├── test_security.py    # Скрипт для тестирования защиты от prompt injection (227 строк)
├── requirements.txt    # Зависимости Python
├── README.md           # Подробная документация (319 строк)
├── README.md           # Документация по заданию 5
└── INSTRUCTIONS.md     # Инструкции по выполнению задания 5
```

### Использование

**Демонстрация работы бота:**
```bash
cd task_4
python demo.py
```

**Автоматическое тестирование:**
```bash
python test_bot.py
```

**Интерактивный режим:**
```bash
python rag_bot.py
```

### Ключевые особенности реализации

1. **Автоматическое извлечение few-shot примеров**: Примеры выбираются из базы знаний на основе релевантности
2. **Гибкое управление CoT**: Можно включать/отключать Chain-of-Thought в зависимости от задачи
3. **Обработка граничных случаев**: Корректная обработка ситуаций, когда информация не найдена
4. **Трассировка источников**: Каждый ответ содержит информацию об источниках (файлы, чанки)
5. **Метаданные запросов**: Сохранение информации об использованных токенах для анализа

### Файлы задания

- `task_4/rag_bot.py` — основной модуль RAG-бота с полной реализацией пайплайна
- `task_4/test_bot.py` — скрипт для автоматического тестирования всех функций
- `task_4/demo.py` — демонстрация работы бота с примерами
- `task_4/requirements.txt` — зависимости Python
- `task_4/README.md` — подробная документация по использованию

### Выводы

✅ **Задание выполнено полностью**:
- Реализован полнофункциональный RAG-бот
- Применены техники Few-shot и Chain-of-Thought
- Все тесты пройдены успешно
- Бот корректно отвечает на вопросы из базы знаний
- Бот честно говорит "Я не знаю" на вопросы вне базы знаний
- Создана полная документация и примеры использования

---

## Задание 5. Запуск и демонстрация работы бота

*Будет заполнено после выполнения задания*

---

## Задание 6. Автоматическое ежедневное обновление базы знаний

### Отчёт

Реализована полностью автоматизированная система обновления базы знаний с отслеживанием изменений, инкрементальным обновлением индекса и подробным логированием.

### Реализованные компоненты

**Основной скрипт обновления** (`update_index.py`):
- ✅ Автоматическое сканирование источника данных (`knowledge_base/`)
- ✅ Отслеживание изменений через SHA256 хеширование файлов
- ✅ Инкрементальное обновление — обрабатываются только новые и изменённые файлы
- ✅ Автоматическое удаление чанков из удалённых файлов
- ✅ Подробное логирование всех операций с временными метками
- ✅ Сохранение состояния между запусками для эффективного отслеживания

**Планировщик задач** (`setup_cron.sh`):
- ✅ Автоматическая настройка cron для периодического запуска
- ✅ Поддержка виртуального окружения Python
- ✅ Настройка логирования в `logs/cron.log`
- ✅ Интерактивная настройка с подтверждением

**Логирование**:
- ✅ Файлы логов с временными метками: `logs/update_YYYYMMDD_HHMMSS.log`
- ✅ Детальная статистика каждого обновления
- ✅ Обработка и логирование ошибок
- ✅ Сохранение состояния в `index_state.json`

### Источник данных

**Выбранный источник**: Локальная папка `knowledge_base/`

**Обоснование**:
- Простота реализации для первого прототипа
- Легко расширяется для других источников (S3, Git, RSS, Google Drive)
- Позволяет протестировать всю логику обновления

**Как происходит подхват новых файлов**:
1. Скрипт сканирует `knowledge_base/` рекурсивно
2. Вычисляет SHA256 хеш каждого файла
3. Сравнивает с предыдущим состоянием (из `index_state.json`)
4. Обрабатывает только файлы с новыми или изменёнными хешами

### Процесс обновления

**Шаг 1: Сканирование**
- Рекурсивный обход `knowledge_base/`
- Исключение служебных папок (`raw`, `__pycache__`, `logs`)
- Исключение служебных файлов (`README.md`, `terms_map.json`)

**Шаг 2: Отслеживание изменений**
- Загрузка предыдущего состояния из `index_state.json`
- Вычисление хешей всех файлов
- Определение новых, изменённых и удалённых файлов

**Шаг 3: Обработка документов**
- Загрузка новых/изменённых документов
- Разбиение на чанки (500 токенов, overlap 100)
- Сохранение метаданных (source, file_name, chunk_index, indexed_at)

**Шаг 4: Генерация эмбеддингов**
- Использование модели `sentence-transformers/all-MiniLM-L6-v2`
- Генерация эмбеддингов для всех новых чанков

**Шаг 5: Обновление векторной БД**
- Добавление новых чанков в ChromaDB
- Удаление чанков из удалённых файлов
- Обновление метаданных

**Шаг 6: Сохранение состояния**
- Сохранение хешей всех файлов
- Сохранение времени последнего обновления
- Сохранение статистики обновления

**Шаг 7: Логирование**
- Запись всех операций в лог-файл
- Вывод статистики в консоль

### Периодический запуск

**Настроенный планировщик**: Cron (macOS/Linux)

**Расписание**: Ежедневно в 6:00 (`0 6 * * *`)

**Настройка**:
```bash
# Автоматическая настройка через скрипт
./setup_cron.sh

# Или ручная настройка
crontab -e
# Добавить: 0 6 * * * /path/to/.venv/bin/python /path/to/task_6/update_index.py >> /path/to/task_6/logs/cron.log 2>&1
```

**Обработка ошибок**:
- При ошибке скрипт возвращает код 1
- Ошибки логируются в файл лога
- Cron автоматически повторяет запуск по расписанию
- Можно настроить уведомления об ошибках (email, Slack)

**Альтернативные варианты**:
- Windows: Task Scheduler
- Docker: встроенный планировщик или cron в контейнере
- Kubernetes: CronJob

### Логирование

**Формат логов**:
- Временные метки для каждой операции
- Уровни логирования (INFO, WARNING, ERROR)
- Статистика обновления (количество файлов, чанков, время выполнения)
- Ошибки с полным traceback

**Пример лога**:
```
2025-01-17 06:00:00 - INFO - Автоматическое обновление векторного индекса
2025-01-17 06:00:01 - INFO - Найдено 173 документов для проверки
2025-01-17 06:00:02 - INFO - Новый файл: /path/to/knowledge_base/new_document.md
2025-01-17 06:00:05 - INFO - Создано 5 чанков из 1 документов
2025-01-17 06:00:10 - INFO - ✅ Добавлено 5 чанков
2025-01-17 06:00:10 - INFO - Новых файлов: 1
2025-01-17 06:00:10 - INFO - Добавлено чанков: 5
2025-01-17 06:00:10 - INFO - Всего чанков после: 4514
2025-01-17 06:00:10 - INFO - Время выполнения: 10.23 секунд
2025-01-17 06:00:10 - INFO - ✅ Обновление завершено успешно
```

**Хранение**:
- Файлы: `logs/update_YYYYMMDD_HHMMSS.log`
- Cron логи: `logs/cron.log`
- Состояние: `index_state.json`

### Архитектурная диаграмма

Создана диаграмма в формате PlantUML (`architecture.puml`), показывающая:

**Компоненты**:
1. Источник данных (`knowledge_base/`)
2. Планировщик (cron)
3. Скрипт обновления (`update_index.py`)
   - Сканер файлов
   - Обработчик текста
   - Генератор эмбеддингов
4. Векторная БД (ChromaDB)
5. Логирование (файлы логов и состояние)

**Поток данных**:
```
knowledge_base → Сканер файлов → Обработчик текста → Генератор эмбеддингов → ChromaDB
                                      ↓
                                  Логирование
```

**Где хранятся логи**: Директория `logs/` с файлами по дате/времени

**Где запускается задача**: Cron (системный планировщик задач)

Для просмотра диаграммы:
1. Откройте `architecture.puml` в онлайн-редакторе PlantUML: http://www.plantuml.com/plantuml/uml/
2. Или используйте расширение PlantUML в VS Code/IntelliJ IDEA

### Результаты тестирования

**Тест 1: Первый запуск (создание индекса)**
- ✅ Успешно создан индекс со всеми документами
- ✅ Состояние сохранено в `index_state.json`
- ✅ Логи записаны корректно

**Тест 2: Добавление нового файла**
- ✅ Новый файл обнаружен
- ✅ Чанки созданы и добавлены в индекс
- ✅ Состояние обновлено

**Тест 3: Изменение существующего файла**
- ✅ Изменение обнаружено через хеш
- ✅ Файл переиндексирован
- ✅ Старые чанки удалены, новые добавлены

**Тест 4: Удаление файла**
- ✅ Удаление обнаружено
- ✅ Чанки из удалённого файла удалены из индекса
- ✅ Состояние обновлено

**Тест 5: Запуск без изменений**
- ✅ Скрипт корректно определяет отсутствие изменений
- ✅ Индекс не пересоздаётся
- ✅ Время выполнения минимально (~1-2 секунды)

### Производительность

**Показатели на тестовой базе** (173 документа, ~4509 чанков):
- Сканирование: ~1-2 секунды
- Обработка нового файла (1 документ, ~5 чанков): ~5-10 секунд
- Полное обновление (все документы): ~30-40 секунд

**Оптимизации**:
- Инкрементальное обновление (только изменённые файлы)
- Кэширование хешей файлов
- Параллельная обработка (можно расширить)

### Структура файлов

```
task_6/
├── update_index.py      # Основной скрипт обновления индекса (450+ строк)
├── setup_cron.sh        # Скрипт настройки cron (macOS/Linux)
├── requirements.txt    # Зависимости Python
├── architecture.puml    # Архитектурная диаграмма (PlantUML)
├── README.md           # Подробная документация (500+ строк)
├── index_state.json    # Состояние индекса (создаётся автоматически)
└── logs/               # Логи обновлений
    ├── update_20250117_060000.log
    ├── update_20250118_060000.log
    └── cron.log        # Логи из cron (если настроен)
```

### Использование

**Ручной запуск**:
```bash
cd task_6
python update_index.py
```

**Настройка автоматического запуска**:
```bash
./setup_cron.sh
```

**Проверка логов**:
```bash
# Последний лог
ls -t logs/update_*.log | head -1 | xargs cat

# Поиск ошибок
grep -i error logs/update_*.log
```

### Ключевые особенности реализации

1. **Эффективное отслеживание изменений**: Использование SHA256 хешей вместо времени модификации (более надёжно)
2. **Инкрементальное обновление**: Обрабатываются только изменённые файлы, что экономит время и ресурсы
3. **Автоматическая очистка**: Удаление чанков из удалённых файлов для поддержания актуальности индекса
4. **Надёжное логирование**: Подробные логи с временными метками для отладки и мониторинга
5. **Сохранение состояния**: Восстановление после сбоев и анализ истории обновлений
6. **Расширяемость**: Легко адаптируется для других источников данных (S3, Git, RSS)

### Файлы задания

- `task_6/update_index.py` — основной скрипт автоматического обновления индекса
- `task_6/setup_cron.sh` — скрипт настройки cron для периодического запуска
- `task_6/architecture.puml` — архитектурная диаграмма в формате PlantUML
- `task_6/README.md` — подробная документация по использованию
- `task_6/requirements.txt` — зависимости Python
- `task_6/index_state.json` — состояние индекса (создаётся автоматически)
- `task_6/logs/` — директория с логами обновлений

### Выводы

✅ **Задание выполнено полностью**:
- Реализован скрипт автоматического обновления индекса с отслеживанием изменений
- Настроено инкрементальное обновление (только новые/изменённые файлы)
- Реализовано автоматическое удаление устаревших чанков
- Настроено подробное логирование всех операций
- Создан скрипт настройки cron для периодического запуска
- Создана архитектурная диаграмма, показывающая поток данных и компоненты
- Написана полная документация с примерами использования

Система готова к использованию в продакшене и может быть легко расширена для работы с другими источниками данных (S3, Git-репозитории, RSS-фиды, Google Drive).

---

## Задание 7. Аналитика покрытия и качества базы знаний

### Отчёт

Реализована полная система оценки полноты и актуальности базы знаний с автоматическим тестированием, логированием запросов и анализом пробелов в покрытии тем.

### Реализованные компоненты

**Скрипт создания пробелов** (`create_gaps.py`):
- ✅ Удаление 3 ключевых сущностей из базы знаний для тестирования
- ✅ Автоматическое создание резервных копий удалённых файлов
- ✅ Скрипт восстановления (`restore_gaps.py`) для возврата файлов после тестирования

**Система логирования** (`evaluate.py`):
- ✅ Автоматическое логирование всех запросов в формате JSONL
- ✅ Сохранение метаданных: timestamp, запрос, найденные чанки, длина ответа, источники
- ✅ Оценка успешности и полноты ответов
- ✅ Статистика использования токенов

**Золотой набор вопросов** (`golden_questions.txt`):
- ✅ 15 вопросов, покрывающих разные аспекты базы знаний
- ✅ 8 вопросов на известные темы (бот должен ответить)
- ✅ 7 вопросов на удалённые/отсутствующие темы (бот должен не ответить)

**Автоматическое тестирование** (`evaluate.py`):
- ✅ Автоматический запуск тестов на золотом наборе вопросов
- ✅ Оценка качества ответов (успешность, полнота)
- ✅ Анализ статистики и выявление пробелов
- ✅ Детальный отчёт с рекомендациями

**Диаграмма последовательности** (`sequence.puml`):
- ✅ Визуализация процесса обработки запроса
- ✅ Показ взаимодействия компонентов системы
- ✅ Обработка ошибок и граничных случаев

### Удалённые сущности

Для создания искусственных пробелов удалены 3 ключевые сущности:

1. **Zara Morningstar** (`39_Charlie_Morningstar.md`) — главный персонаж, основатель Twilight Manor
2. **Korax** (`88_Alastor.md`) — важный персонаж, Signal Netherspawn
3. **Twilight Manor** (`138_Hazbin_Hotel.md`) — ключевая локация, главный отель

**Обоснование выбора:**
- Это центральные сущности вселенной, упоминаемые во многих документах
- Удаление их создаёт значительные пробелы, которые легко обнаружить
- Позволяет протестировать поведение бота при отсутствии критически важной информации

### Формат логирования

Каждый запрос логируется в `logs.jsonl` (JSONL формат) со следующими полями:

- `timestamp` — время запроса (ISO 8601)
- `query` — текст запроса
- `chunks_found` — количество найденных релевантных чанков
- `answer_length` — длина ответа в символах
- `is_successful` — флаг успешности ответа (соответствует ожидаемому результату)
- `completeness_score` — оценка полноты ответа (0.0-1.0)
- `should_answer` — должен ли бот ответить на этот вопрос
- `sources` — список источников (имена файлов)
- `answer` — полный текст ответа
- `filtered_chunks` — количество отфильтрованных злонамеренных чанков
- `input_tokens`, `output_tokens`, `total_tokens` — статистика использования токенов

### Золотой набор вопросов

**Вопросы на известные темы (бот должен ответить):**
1. Кто такой Silk Shadow?
2. Что такое Starfall City?
3. Что такое Purge?
4. Опиши персонажа Grim
5. Где находится Feast District?
6. Что такое Reformation?
7. Кто такой Malachar Morningstar?
8. Опиши Netherrealm

**Вопросы на удалённые/отсутствующие темы (бот должен не ответить):**
1. Кто такая Zara Morningstar? (удалена)
2. Кто такой Korax? (удалён)
3. Что такое Twilight Manor? (удалена)
4. Кто такой Гарри Поттер? (отсутствует)
5. Что такое квантовая физика? (отсутствует)
6. Где находится Марс? (отсутствует)
7. Опиши персонажа Дарта Вейдера (отсутствует)

### Процесс оценки

**Оценка успешности ответа:**
- Для вопросов, на которые бот должен ответить: проверяется, что бот не сказал "не знаю" и нашёл релевантные чанки
- Для вопросов, на которые бот должен не ответить: проверяется, что бот сказал "не знаю" или не нашёл чанки

**Оценка полноты ответа:**
- Анализируется длина ответа (оптимально 50-500 символов)
- Учитывается информативность и наличие конкретных фактов
- Оценка от 0.0 (неполный) до 1.0 (полный)

### Результаты анализа

После запуска `evaluate.py` система автоматически:

1. **Вычисляет статистику:**
   - Общий процент успешности ответов
   - Процент успешности по категориям (должен ответить / должен не ответить)
   - Средняя оценка полноты ответов

2. **Выявляет пробелы:**
   - Список вопросов, на которые бот не ответил корректно
   - Темы, по которым база знаний неполная
   - Вопросы, требующие расширения базы знаний

3. **Формирует рекомендации:**
   - Какие темы нужно добавить в базу знаний
   - Где бот выдаёт нерелевантные источники
   - Где нужно улучшить качество документации

### Диаграмма последовательности

Создана диаграмма в формате PlantUML (`sequence.puml`), показывающая:

**Основной поток:**
1. Инициализация бота и загрузка золотого набора вопросов
2. Для каждого вопроса:
   - Преобразование запроса в эмбеддинг
   - Поиск релевантных чанков в векторном индексе
   - Формирование промпта с контекстом
   - Генерация ответа через LLM
   - Оценка качества ответа
   - Логирование результата
3. Анализ всех записей лога и формирование отчёта

**Обработка ошибок:**
- Ошибка при загрузке индекса
- Ошибка при поиске
- Ошибка при генерации ответа
- Пустой индекс
- Невалидный чанк

**Где хранятся логи:** Файл `logs.jsonl` в директории `task_7/`

**Где запускается задача:** Скрипт `evaluate.py` запускается вручную или может быть интегрирован в CI/CD

### Структура файлов

```
task_7/
├── create_gaps.py          # Скрипт для создания искусственных пробелов
├── restore_gaps.py          # Скрипт для восстановления удалённых файлов
├── golden_questions.txt     # Золотой набор вопросов (15 вопросов)
├── evaluate.py             # Скрипт автоматического тестирования и оценки
├── sequence.puml           # Диаграмма последовательности (PlantUML)
├── requirements.txt        # Зависимости Python
├── README.md               # Подробная документация
├── logs.jsonl              # Лог запросов (создаётся автоматически)
└── backup_removed_files/   # Резервные копии удалённых файлов
```

### Использование

**Создание пробелов в базе знаний:**
```bash
cd task_7
python create_gaps.py
```

**Запуск автоматического тестирования:**
```bash
python evaluate.py
```

**Просмотр результатов:**
```bash
# Просмотр лога
cat logs.jsonl | jq '.'

# Поиск неудачных запросов
cat logs.jsonl | jq 'select(.is_successful == false)'
```

**Восстановление файлов после тестирования:**
```bash
python restore_gaps.py
```

### Ключевые особенности реализации

1. **Систематическая оценка качества**: Автоматическое тестирование на заранее подготовленном наборе вопросов
2. **Детальное логирование**: Сохранение всех метаданных для последующего анализа
3. **Выявление пробелов**: Автоматическое определение тем, требующих расширения базы знаний
4. **Обратная совместимость**: Возможность восстановления удалённых файлов после тестирования
5. **Расширяемость**: Легко добавить новые вопросы в золотой набор
6. **Визуализация**: Диаграмма последовательности для понимания процесса

### Выводы

✅ **Задание выполнено полностью**:
- Созданы искусственные пробелы в базе знаний (удалены 3 ключевые сущности)
- Реализована система логирования всех запросов в формате JSONL
- Сформирован золотой набор из 15 вопросов
- Создан скрипт автоматического тестирования с оценкой качества
- Реализован анализ логов и выявление пробелов
- Создана диаграмма последовательности, показывающая процесс обработки запроса

**Рекомендации по улучшению базы знаний:**
1. Добавить информацию о Zara Morningstar, Korax и Twilight Manor (после восстановления файлов)
2. Расширить документацию по персонажам, которые часто упоминаются в вопросах
3. Улучшить покрытие концепций (Purge, Reformation) для более полных ответов
4. Регулярно запускать автоматическое тестирование для отслеживания качества

Система готова к использованию в продакшене и может быть интегрирована в CI/CD для непрерывного мониторинга качества базы знаний.

